import React from 'react';
import classNames from 'classnames/bind';
import styles from './Button.module.scss';

const cx = classNames.bind(styles);

function Button({}) {

    let comp = 'button';

    return <comp>Button</comp>;
}

export default Button;
